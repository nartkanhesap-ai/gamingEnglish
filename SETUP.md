# Gaming English — Word Updater Config
# Bu dosyayı kopyala: cp .env.example .env
# Gerçek değerleri gir, .gitignore'a ekle!

# Claude API anahtarı (claude.ai/settings/keys)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Firebase proje ID
FIREBASE_PROJECT_ID=english-coaching-2eeee

# Firebase Admin SDK service account JSON dosyasının yolu
# Firebase Console → Proje Ayarları → Hizmet Hesapları → Yeni özel anahtar oluştur
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json
